

function SliderComponent({name, contact, place}) {
  return (
    <div>
      <div>{name}</div>
      <div>{contact}</div>
      <div>{place}</div>
   
    </div>
  );
}

export default SliderComponent;
